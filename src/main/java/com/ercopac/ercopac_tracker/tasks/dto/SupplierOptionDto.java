package com.ercopac.ercopac_tracker.tasks.dto;

/** Active organisation supplier returned solely for task assignment selection. */
public record SupplierOptionDto(Long id, String code, String name) {
}
