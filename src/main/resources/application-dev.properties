# Development-only fixtures. These records are idempotent and are never loaded
# unless the dev profile is explicitly selected.
projectum.seed.local=true
app.seed.enabled=false
spring.jpa.hibernate.ddl-auto=update
